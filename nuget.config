<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!--
    PepperDash.ZoomRoom.Sdk sandbox builds are published to the pepperdash-beincourt GitHub Packages
    feed by the zoom-room-sdk-lib fork's CI. The build workflow authenticates the GitHub source with
    PACKAGES_PAT (falls back to GITHUB_TOKEN); everything else still resolves from nuget.org.
    Package source mapping keeps every other dependency off the authenticated feed.
  -->
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
    <add key="github-pepperdash-beincourt" value="https://nuget.pkg.github.com/pepperdash-beincourt/index.json" />
  </packageSources>
  <packageSourceMapping>
    <packageSource key="nuget.org">
      <package pattern="*" />
    </packageSource>
    <packageSource key="github-pepperdash-beincourt">
      <package pattern="PepperDash.ZoomRoom.Sdk" />
    </packageSource>
  </packageSourceMapping>
</configuration>
